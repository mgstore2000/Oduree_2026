{
  "name": "oduree-theme",
  "version": "1.0.0",
  "description": "Oduree Shopify Theme",
  "scripts": {
    "dev": "shopify theme dev -e development",
    "pull": "shopify theme pull -e development",
    "push": "shopify theme push -e development",
    "dev:live": "shopify theme dev -e live",
    "pull:live": "shopify theme pull -e live",
    "push:live": "shopify theme push -e live"
  },
  "private": true
}
